"use strict";
const accessName = "juana";

if (accessName === "ana" || accessName === "carmen") {
  console.log(`bievenida ${accessName}`);
} else {
  console.log("Lo siento pero el usuario no está registrado");
}
